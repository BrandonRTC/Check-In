<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

	  <title>Brandon BedCheck</title>

    <%= stylesheet_link_tag    "application" %>
		<%= stylesheet_link_tag "print", media: "print" %>
    <%= javascript_include_tag "application" %>
    <%= csrf_meta_tags %>
  </head>

  <body class="<%= controller_name %> <%= action_name %>">
		<% if !current_user.nil? %>
			<div class="top-bar" data-topbar role="navigation">
          <div class="top-bar-left">
            <h4>Brandon Bed Check</h4>
          </div>

          <div class="top-bar-right">
            <ul class="menu wide-breakpoint">
              <li><a href="<%= user_url(current_user.id) %>">New Tour</a></li>
              <li class="divider">|</li>
              <% if current_user.superuser? %>
              <li><a href="<%= houses_url %>">In Progress Tours</a></li>
              <li class="divider">|</li>
              <li><a href="<%= check_ins_url %>">Check-In Review</a></li>
              <li class="divider">|</li>
              <li><a href="<%= tours_url %>">Tour Review</a></li>
              <% end %>
              <li class="has-form">
                <form action="<%= session_url %>" method="post">
                  <input type="hidden" name="_method" value="delete">
                  <%= auth_token %>
                  <button class="button small alert">Logout</button>
                </form>
              </li>
            </ul>

            <ul class="dropdown menu medium-breakpoint" data-dropdown-menu>
              <li>
                <a href="#">Admin</a>
                <ul class="menu dropdown-submenu">
                  <li><a href="<%= user_url(current_user.id)%>">New Tour</a></li>
                  <li><a href="<%= houses_url %>">In Progress Tours</a></li>
                  <li><a href="<%= check_ins_url %>">Check-In Review</a></li>
                  <li><a href="<%= tours_url %>">Tour Review</a></li>
                </ul>
              </li>
            </ul>
          </div>
			</div>
		<% end %>

		<% if flash.now[:errors] %>
		<div class="row">
			<section id="errors" class="small-8 columns small-centered">
				<% flash.now[:errors].each do |e| %>
				<%= e %>
				<br>
				<% end %>
			</section>
		</div>
		<% end %>

    <%= yield %>

    <%= javascript_include_tag "what-input.min" %>
    <%= javascript_include_tag "foundation" %>
    <%= javascript_include_tag "foundation_app" %>
  </body>
</html>
