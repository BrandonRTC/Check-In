<!DOCTYPE html>
<html>
<head>
  <title>BrandonApp</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>

<% if flash.now[:errors] %>
	<%= flash.now[:errors] %>
<% end %>

<!-- add optional navbar for admins (rotate between current tours and records of check-ins) -->
<!-- tours url and check_in url might need to point to API routes later -->

<% if !current_user.nil? %>
	<ul>
		<li><a href="<%= user_url(current_user.id) %>">Logged in as: <%= current_user.username %></li>
		<% if current_user.superuser? %>
			<li><a href="<%= tours_url %>">Tour List</a></li>
			<li><a href="<%= check_ins_url %>">Check-In List</a></li>
		<% end %>
		<li><%= button_to "Logout", session_url, method: :delete %></li>
	</ul>
<% end %>

<%= yield %>


</body>
</html>
