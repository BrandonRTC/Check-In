<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

	  <title>Brandon Check-In</title>
    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "application" %>
    <%= csrf_meta_tags %>
  </head>

  <body>

		<% if flash.now[:errors] %>
			<%= flash.now[:errors] %>
		<% end %>

		<% if !current_user.nil? %>
			<ul class="no-bullet inline-list">
				<li><a href="<%= user_url(current_user.id) %>"><%= current_user.username %></li>
				<% if current_user.superuser? %>
					<li><a href="<%= tours_url %>">Current Tours</a></li>
					<li><a href="<%= check_ins_url %>">Check-In Review</a></li>
					<li><a href="">Tour Review</a></li>
				<% end %>
				<li>
					<!-- redo this as an anchor tag -->
					<form action="<%= session_url %>" method="post">
						<input type="hidden" name="_method" value="delete">
						<%= auth_token %>
						<button class="button tiny">Logout</button>
					</form>
				</li>
			</ul>
		<% end %>

    <%= yield %>

  </body>
</html>